43. 조건에 맞는 사용자와 총 거래금액 조회하기

(1). 코드

SELECT USER_ID, NICKNAME, SUM(PRICE) AS TOTAL_SALES
FROM USED_GOODS_BOARD USED_BOARD INNER JOIN USED_GOODS_USER USED_USER ON USED_BOARD.WRITER_ID = USED_USER.USER_ID
WHERE STATUS = 'DONE'
GROUP BY USER_ID
HAVING TOTAL_SALES>=500000
ORDER BY 3
